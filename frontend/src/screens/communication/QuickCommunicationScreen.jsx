import React, { useState } from 'react'; import { StyleSheet, Text, TouchableOpacity } from 'react-native';
import FeatureScreen from '../../components/common/FeatureScreen'; import SpeechButton from '../../components/communication/SpeechButton'; import { useTheme } from '../../theme';
const PHRASES=['I need help','I need space',"I can't speak",'Please wait for me','Please say that again','It is too loud'];
export default function QuickCommunicationScreen({navigation}){const {theme}=useTheme();const [selected,setSelected]=useState('');return <FeatureScreen navigation={navigation} title="Quick communication" subtitle="One tap for urgent everyday messages">{PHRASES.map((p,i)=><TouchableOpacity key={p} accessibilityRole="button" accessibilityHint="Select and speak this phrase" onPress={()=>setSelected(p)} style={[styles.button,{backgroundColor:i<3?theme.colors.primary:theme.colors.surface,borderColor:theme.colors.primary,borderRadius:theme.borderRadius.lg}]}><Text style={[styles.text,{color:i<3?'#fff':theme.colors.text}]}>{p}</Text></TouchableOpacity>)}{selected?<SpeechButton text={selected} size="large" style={{marginTop:12}}/>:null}</FeatureScreen>}; const styles=StyleSheet.create({button:{minHeight:72,justifyContent:'center',padding:18,borderWidth:2,marginBottom:12},text:{fontSize:20,fontWeight:'700',textAlign:'center'}});
