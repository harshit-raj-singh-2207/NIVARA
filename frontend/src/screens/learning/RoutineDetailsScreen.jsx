import React, { useEffect } from 'react'; import { Text } from 'react-native'; import FeatureScreen, { SectionTitle } from '../../components/common/FeatureScreen'; import AppCard from '../../components/common/AppCard'; import EmptyState from '../../components/common/EmptyState'; import ProgressBar from '../../components/learning/ProgressBar'; import TaskCard from '../../components/learning/TaskCard'; import useLearningStore from '../../store/learningStore'; import { LEARNING_ROUTES } from '../../constants/routes'; import { useTheme } from '../../theme';
export default function RoutineDetailsScreen({navigation,route}){const {theme}=useTheme();const s=useLearningStore();useEffect(()=>{if(!s.routines.length)s.fetchRoutines()},[]);const routine=s.routines.find(x=>x.id===(route.params?.routineId||s.activeRoutineId))||s.routines[0];if(!routine)return <FeatureScreen navigation={navigation} title="Routine details"><EmptyState title="Routine not found" actionTitle="Try again" onActionPress={s.fetchRoutines}/></FeatureScreen>;const steps=routine.tasks.flatMap(t=>t.steps||[]),done=steps.filter(x=>x.completed).length,current=routine.tasks.find(t=>t.steps?.some(x=>!x.completed));return <FeatureScreen navigation={navigation} title={routine.title} subtitle={routine.time}><Text style={{color:theme.colors.textSecondary}}>Follow one small task at a time. If plans change, pause and choose the next available task.</Text><SectionTitle>Progress: {done} of {steps.length} steps</SectionTitle><ProgressBar progress={steps.length?done/steps.length:0}/>{current?<AppCard style={{marginVertical:14}}><Text style={{color:theme.colors.textMuted}}>CURRENT TASK</Text><Text style={{color:theme.colors.text,fontSize:18,fontWeight:'700',marginTop:4}}>{current.title}</Text></AppCard>:null}<SectionTitle>Ordered tasks</SectionTitle>{routine.tasks.map(t=><TaskCard key={t.id} task={t} onPress={()=>navigation.navigate(LEARNING_ROUTES.TASK_DETAILS,{taskId:t.id,routineId:routine.id})} onToggleStep={s.toggleStepCompletion}/>)}</FeatureScreen>}
