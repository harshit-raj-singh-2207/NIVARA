import React from 'react'; import { StyleSheet, Text, TouchableOpacity } from 'react-native'; import FeatureScreen, { SectionTitle } from '../../components/common/FeatureScreen'; import { LEARNING_ROUTES } from '../../constants/routes'; import { useTheme } from '../../theme';
const TOPICS=[['Understanding emotions',70,true],['Everyday money',35,true],['Safe online choices',10,false],['Reading social messages',0,false]];
export default function LearningTopicsScreen({navigation}){const {theme}=useTheme();return <FeatureScreen navigation={navigation} title="Learning topics" subtitle="Choose a topic that fits you"><SectionTitle>Recommended for you</SectionTitle>{TOPICS.map(([title,progress,recommended])=><TouchableOpacity accessibilityRole="button" key={title} onPress={()=>navigation.navigate(LEARNING_ROUTES.TUTOR,{topic:title})} style={[styles.card,{backgroundColor:theme.colors.surface,borderColor:recommended?theme.colors.primary:theme.colors.border,borderRadius:theme.borderRadius.lg}]}><Text style={{color:theme.colors.text,fontSize:17,fontWeight:'700'}}>{title}</Text><Text style={{color:theme.colors.textSecondary,marginTop:6}}>{progress}% complete{recommended?' · Recommended':''}</Text></TouchableOpacity>)}</FeatureScreen>};const styles=StyleSheet.create({card:{minHeight:78,padding:16,borderWidth:1.5,marginBottom:12,justifyContent:'center'}});
